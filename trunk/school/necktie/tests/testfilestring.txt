Func sqrt( num ) 
    n = 0; 
    n=0; 
    n = 0;n=0; If(EQ(num, n)) to endit; N = num / 2 + 1; N1 = ( n+ ( num / n )) / 2; Again:    If ( GE( n1, n ) ) to endit; n = n1; N1 = ( n + ( num / n )) / 2; If ( TRUE ) to again; Endit:    sqrt = n; Endfunc Func main() Write( "Enter a number:" ); Read( X ); S = sqrt( x ); Write("The square root is:" ); Write( x ); Endfunc

Func sqrt( num )
    n = 0;
    n=0;
    n = 0;n=0;
    If(EQ(num, n)) to endit;N = num / 2 + 1; N1 = ( n+ ( num / n )) / 2;Again:    If ( GE( n1, n ) ) to endit;n = n1;N1 = ( n + ( num / n )) / 2;If ( TRUE ) to again;Endit:    sqrt = n; EndfuncFunc main()Write( "Enter a number:" );Read( X );S = sqrt( x );Write("The square root is:" );Write( x );Endfunc
